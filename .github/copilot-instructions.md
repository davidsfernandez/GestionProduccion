# Copilot Instructions

## General Guidelines
- Prefer using cmd.exe or Bash (Ubuntu/WSL) for executing commands instead of PowerShell, as PowerShell can cause issues with redirections, syntax, and special characters.